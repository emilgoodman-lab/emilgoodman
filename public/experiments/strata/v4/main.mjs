import {StrataWorld,GLYPHS,STEP} from './physics.mjs';
import {StrataAudio} from './audio.mjs';
const canvas=document.querySelector('#strata'),ctx=canvas.getContext('2d',{alpha:false}),main=document.querySelector('main'),readout=document.querySelector('#readout'),mode=document.querySelector('#mode'),soundButton=document.querySelector('#sound'),pauseButton=document.querySelector('#pause'),audio=new StrataAudio(),reduced=matchMedia('(prefers-reduced-motion: reduce)');
let world,width=1,height=1,dpr=1,raf=0,last=0,accumulator=0,lastReadout=-1,paused=reduced.matches,hand=false,handFist=false,pointerId=null,spaceDown=false;
await document.fonts.load('32px LabSymbols');
const atlas=GLYPHS.map(glyph=>{const c=document.createElement('canvas');c.width=c.height=56;const a=c.getContext('2d');a.fillStyle='#e8e8e8';a.font='38px LabSymbols';a.textAlign='center';a.textBaseline='middle';a.fillText(glyph,28,27);return c;});
function draw(){ctx.setTransform(dpr,0,0,dpr,0,0);ctx.fillStyle='#090909';ctx.fillRect(0,0,width,height);for(const p of world.particles){const size=p.r*2.05;ctx.save();ctx.translate(p.x,p.y);ctx.rotate(p.angle);ctx.globalAlpha=p.shade;ctx.drawImage(atlas[p.glyph],-size/2,-size/2,size,size);ctx.restore();}
 if(world.pointer.active){const p=world.pointer;ctx.globalAlpha=.76;ctx.strokeStyle='#929292';ctx.lineWidth=.7;ctx.setLineDash([2,5]);ctx.beginPath();ctx.arc(p.x,p.y,world.broomRadius,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);ctx.strokeRect(p.x-3,p.y-3,6,6);if(world.held){const length=34+world.windStrength*42;ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(p.x+Math.cos(world.windAngle)*length,p.y+Math.sin(world.windAngle)*length);ctx.stroke();}}
 ctx.globalAlpha=1;main.classList.toggle('inverted',world.held);
}
function telemetry(force=false){if(!force&&world.time-lastReadout<.18)return;lastReadout=world.time;readout.textContent=[`grains  = 5000 / 3 LAYERS`,`split   = 1667 / 1667 / 1666`,`scale   = 2.0 × / ±25%`,`signs   = ${GLYPHS.length} permanent`,`moving  = ${world.moving}`,`contact = ${world.contactsByLayer.join(' / ')}`,`exclude = ${world.sweepActivity.toFixed(3)}`,`wind    = ${world.windStrength.toFixed(3)}`,`bearing = ${(world.windAngle*180/Math.PI).toFixed(0)}° / ${world.windPan.toFixed(2)}`,`input   = ${hand?'HAND':'POINTER'}`].join('\n');mode.textContent=world.held?'TURBINE / HELD / INVERSE':world.pointer.active?'EXCLUSION RING':'TABLE REST';}
function resize(){const b=canvas.getBoundingClientRect();width=b.width;height=b.height;dpr=Math.min(devicePixelRatio||1,2);canvas.width=Math.round(width*dpr);canvas.height=Math.round(height*dpr);world?.resize(width,height);if(!world)world=new StrataWorld(width,height);draw();telemetry(true);}
function tick(now){raf=0;if(paused||document.hidden){last=0;return;}accumulator+=last?Math.min(.06,(now-last)/1000):STEP;last=now;let steps=0;while(accumulator>=STEP&&steps<7){world.step();accumulator-=STEP;steps++;}audio.update(world);draw();telemetry();raf=requestAnimationFrame(tick);}
function start(){if(!raf&&!paused&&!document.hidden)raf=requestAnimationFrame(tick);}
function setMode(){world.setHeld(pointerId!==null||spaceDown||handFist);draw();telemetry(true);}
function pause(value){paused=value;pointerId=null;spaceDown=false;handFist=false;world.release();last=0;accumulator=0;audio.setPaused(value);pauseButton.textContent=value?'[ RESUME ]':'[ PAUSE ]';pauseButton.setAttribute('aria-pressed',String(value));if(value){cancelAnimationFrame(raf);raf=0;draw();}else start();telemetry(true);}
function position(e){if(hand)return;const b=canvas.getBoundingClientRect();world.move(e.clientX-b.left,e.clientY-b.top);}
canvas.addEventListener('pointermove',e=>{if(pointerId===null||e.pointerId===pointerId)position(e);});canvas.addEventListener('pointerdown',e=>{if(pointerId!==null||e.button!==0)return;pointerId=e.pointerId;canvas.setPointerCapture(pointerId);position(e);setMode();});
function pointerUp(e){if(e.pointerId!==pointerId)return;pointerId=null;setMode();if(e.pointerType==='touch'&&!hand)world.pointer.active=false;}window.addEventListener('pointerup',pointerUp);window.addEventListener('pointercancel',pointerUp);canvas.addEventListener('lostpointercapture',()=>{if(pointerId!==null){pointerId=null;setMode();}});canvas.addEventListener('pointerleave',()=>{if(pointerId===null&&!hand)world.pointer.active=false;});
canvas.addEventListener('keydown',e=>{if(e.key===' '){e.preventDefault();spaceDown=true;setMode();}else if(e.key.toLowerCase()==='p'){e.preventDefault();if(!e.repeat)pause(!paused);}else if(e.key.startsWith('Arrow')){e.preventDefault();const p=world.pointer;world.move(p.x+(e.key==='ArrowRight'?24:e.key==='ArrowLeft'?-24:0),p.y+(e.key==='ArrowDown'?24:e.key==='ArrowUp'?-24:0));}});window.addEventListener('keyup',e=>{if(e.key===' '){spaceDown=false;setMode();}});
window.addEventListener('lab-hand',e=>{hand=Boolean(e.detail.tracked);handFist=hand&&Boolean(e.detail.fist);if(hand)world.move((e.detail.x+1)*width/2,(e.detail.y+1)*height/2);else if(pointerId===null)world.pointer.active=false;setMode();});window.addEventListener('blur',()=>{pointerId=null;spaceDown=false;handFist=false;hand=false;world.release();draw();telemetry(true);});
soundButton.addEventListener('click',async()=>{try{if(audio.enabled){audio.mute();soundButton.textContent='[ SOUND ]';soundButton.setAttribute('aria-pressed','false');}else{await audio.enable();audio.setVolume(Number(document.querySelector('#volume').value));audio.setPaused(paused);soundButton.textContent='[ MUTE ]';soundButton.setAttribute('aria-pressed','true');}}catch{mode.textContent='AUDIO UNAVAILABLE';}});document.querySelector('#volume').addEventListener('input',e=>audio.setVolume(Number(e.target.value)));pauseButton.addEventListener('click',()=>pause(!paused));
document.addEventListener('visibilitychange',()=>{pointerId=null;spaceDown=false;handFist=false;hand=false;world.release();if(document.hidden){cancelAnimationFrame(raf);raf=0;last=0;accumulator=0;audio.ctx?.suspend();}else{if(audio.enabled)audio.ctx?.resume();start();}draw();telemetry(true);});window.addEventListener('pagehide',()=>audio.dispose());reduced.addEventListener('change',e=>pause(e.matches));new ResizeObserver(resize).observe(canvas);resize();pause(paused);start();
